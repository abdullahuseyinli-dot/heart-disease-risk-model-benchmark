# Archived latency-simulation bundle

This directory belongs to the archived v1 systems experiment. The optional
`lgbm_deployment_bundle.joblib` was used only to reproduce a local FastAPI
latency simulation; it was not clinically validated and is not a deployable
medical model. The fitted bundle is intentionally not committed to the public
repository.
